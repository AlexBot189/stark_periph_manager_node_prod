# 外骨骼 stark_periph_manager_node 框架运行流程图

> **怎么用（draw.io 导入）**：打开 [draw.io](https://app.diagrams.net) → 菜单 `Arrange → Insert → Advanced → Mermaid` → 粘贴下面任意一个 ```mermaid 代码块 → 生成图 → `File → Export` 或直接复制粘贴到 Word。
>
> **说明**：
> - 每个流程节点里标注了 **调用的函数** 和 **下发的 CAN 指令**（SDO/PDO/NMT + COB-ID + 数据）。
> - ⚠️ 标记 = 该指令失败（返回错误 / 超时不响应）会导致**初始化/流程失败**；无标记 = best-effort，失败只打日志跳过。

---

## 图 1：进程启动 → 等上线 → 校准 → 运行（总览）

```mermaid
flowchart TD
    A["main() 进程启动<br/>解析 -c 配置路径"] --> B["InitDispatcher()<br/>读配置 LoadMotorConfig<br/>CANFD建链 motor_hal_create/init<br/>注册电机 motor_hal_add_motor<br/>启动接收线程 motor_hal_recv_start<br/>打开SHM + 初始化IMU/足压"]
    B --> C["状态 state_transition(STATE_BOOTING)"]
    C --> D["主循环 main_loop_run<br/>轮询电机上线状态"]
    D --> E["电机启动序列<br/>motor_startup_full（见 图2）"]
    E --> F{"所有电机在线?<br/>all_motors_online"}
    F -->|"是"| G["state_transition(STATE_READY)"]
    F -->|"5秒超时"| H["降级 READY<br/>部分电机在线(打WARN)"]
    G --> I["等待校准触发<br/>按键长按 calib_requested"]
    H --> I
    I --> J["校准 motor_calib_start/poll（见 图3）"]
    J --> K{"校准结果?"}
    K -->|"DONE"| L["state_transition(STATE_RUNNING)<br/>RT 1kHz 控制循环激活"]
    K -->|"TIMEOUT/FAILED"| M["状态 FAULT / 重新等待校准"]
    M --> I
```

---

## 图 2：电机启动初始化序列（motor_startup_full，逐个 CAN 指令）

> 这是"初始化下发了哪些 CAN 指令"的核心。函数实现在 `motor_hal_startup.c`。

```mermaid
flowchart TD
    A["电机注册 motor_hal_add_motor"] --> B["启动序列 motor_startup_full(cfg)"]
    B --> C["1. 等 Bootup 帧<br/>CAN: 0x700+ID, data=0<br/>函数: motor_startup_wait_bootup<br/>500ms 试探, 收不到也不失败"]
    C --> D["2. NMT Start<br/>CAN: 0x000, cmd=0x01<br/>函数: canopen_nmt_build + can_driver_send<br/>切 Operational, 不检查响应"]
    D --> E["3. SDO 写心跳周期<br/>CAN: 0x600+ID 写 0x1017=heartbeat_ms<br/>函数: sdo_write_simple<br/>⚠️ 同步等应答, 超时/错误 → 初始化失败"]
    E --> F["4. SDO 关看门狗<br/>CAN: 0x600+ID 写 0x2650=1<br/>函数: sdo_write_simple<br/>best-effort 不检查(仅 disable_watchdog=true 时)"]
    F --> G["5. SDO 读固件版本<br/>CAN: 0x600+ID 读 0x100A<br/>函数: sdo_read_simple<br/>best-effort 失败仅打印跳过"]
    G --> H["6. SDO 配置运动参数<br/>写 0x6081速度 / 0x6083加速 / 0x6084减速<br/>函数: sdo_write_simple<br/>best-effort"]
    H --> I{"auto_enable 配置为 true?"}
    I -->|"否"| J["7. 配置 TPDO 同步上报<br/>写 0x1800/0x1A00 映射<br/>Statusword+Position+Velocity+Current<br/>函数: sdo_write_simple, best-effort"]
    I -->|"是"| K["7. DS402 使能 motor_startup_enable<br/>0x6040=0x0006(Shutdown)<br/>0x6040=0x0007(SwitchOn)<br/>0x6040=0x000F(EnableOp)<br/>⚠️ 任一步失败 → 初始化失败<br/>延时120ms等抱闸释放"]
    K --> J
    J --> L["电机在线, 进入 READY 判定"]
```

---

## 图 3：校准流程（motor_calib）

```mermaid
flowchart TD
    A["校准启动 motor_calib_start"] --> B["Step1 停机<br/>SDO 写 0x6040=0x0006 左右电机<br/>函数: motor_hal_sdo_write<br/>⚠️ 失败 → 校准失败"]
    B --> C["Step2 设零位<br/>函数: motor_hal_set_zero<br/>⚠️ 失败 → 校准失败"]
    C --> D["进入 CHECKING 状态<br/>记录 start_us"]
    D --> E["轮询 motor_calib_poll"]
    E --> F{"超时? timeout_ms"}
    F -->|"是"| G["TIMEOUT 校准失败"]
    F -->|"否"| H["读反馈 motor_hal_get_feedback<br/>等反馈帧可用(0x300)"]
    H --> I{"位置在 ±阈值内?<br/>angle=position×180/32768"}
    I -->|"否"| E
    I -->|"是"| J["力矩零漂标定 motor_hal_torque_calib<br/>best-effort 失败跳过"]
    J --> K["设置控制模式 motor_hal_set_mode<br/>⚠️ 失败 → FAILED"]
    K --> L["DS402 使能<br/>0x6040=0x06→0x07→0x0F<br/>⚠️ 失败 → TIMEOUT"]
    L --> M["启动传感器透传 motor_hal_sensor_config<br/>period_div=4 最快周期"]
    M --> N["DONE 校准完成 → RUNNING"]
```

---

## 图 4：数据上报流程

```mermaid
flowchart TD
    A["电机周期上报反馈帧<br/>CAN: 0x300+ID (12字节)"] --> B["接收线程 motor_hal recv 线程<br/>SocketCAN 收帧"]
    B --> C["解析 feedback_parser<br/>canopen_parse_feedback<br/>位置/速度/电流/力矩/温度/状态"]
    C --> D["写反馈缓存 motor_hal_get_feedback<br/>(PI mutex 保护)"]
    D --> E["RT 线程 PublishFeedback<br/>读缓存 + IMU + 足压"]
    E --> F["写 SHM 双 buffer<br/>fb_buffer + active_idx 切换"]
    F --> G["周期上报 motor_hal 透传<br/>0x6C0+ID 聚合帧(32B)<br/>或 unified_6c0"]
    G --> H["消费者读取<br/>ROS / Web / demo_algo 读 SHM"]
```

---

## 图 5：控制流程（demo_algo 命令 → RT 下发 PDO/SDO）

> demo_algo 命令经 `stark_client`（写 SHM mailbox）→ RT 线程 `ProcessMailbox` → PDO/SDO 下发。

```mermaid
flowchart TD
    A["demo_algo 命令"] --> B{"命令类型"}
    B -->|"使能"| C["stark_enable → SHM STARK_CMD_ENABLE<br/>RT: motor_hal_pdo_enable<br/>PDO 0x100+ID byte0"]
    B -->|"失能"| D["SHM STARK_CMD_DISABLE<br/>RT: motor_hal_pdo_disable<br/>PDO 0x100+ID byte0 + 电流清零"]
    B -->|"电流 cur"| E["stark_torque → STARK_CMD_TORQUE<br/>RT: motor_hal_multi_ctrl 电流模式<br/>PDO 0x200 多轴广播"]
    B -->|"位置 pos"| F["stark_pp → STARK_CMD_PP<br/>RT: 轮廓位置模式 PDO<br/>目标位置(角度→count)"]
    B -->|"力矩 tq"| G["stark_torque_ctrl → STARK_CMD_TORQUE_CTRL<br/>RT: 力矩环 PDO<br/>val 单位 0.05N.m"]
    B -->|"MIT mit"| H["stark_mit → STARK_CMD_MIT<br/>RT: motor_hal_mit_control<br/>PDO 0x110+ID (pos/vel/kp/kd/tq)"]
    C --> I["等反馈帧 0x300 确认<br/>demo_algo _wait_new_fb_frame"]
    D --> I
    E --> I
    F --> I
    G --> I
    H --> I
    I --> J{"反馈超时?"}
    J -->|"是"| K["打印 timeout 提示"]
    J -->|"否"| L["打印控制结果(电流/位置/力矩/状态)"]
```

---

## 附录：CAN 帧 COB-ID 速查表

| COB-ID | 类型 | 方向 | 用途 |
|--------|------|------|------|
| 0x000 | NMT | 主机→电机 | Start(0x01)/Stop/Reset |
| 0x080 | SYNC | 主机→电机 | 同步报文 |
| 0x080+ID | EMCY | 电机→主机 | 紧急报文(故障) |
| 0x600+ID | SDO 请求 | 主机→电机 | 读/写对象字典 |
| 0x580+ID | SDO 应答 | 电机→主机 | SDO 响应 |
| 0x700+ID | Bootup/Heartbeat | 电机→主机 | 上线(data=0)/心跳 |
| 0x100+ID | 自定义 PDO | 主机→电机 | 单轴实时控制(7B) |
| 0x110+ID | MIT PDO | 主机→电机 | MIT 阻抗控制(9B) |
| 0x200 | 多轴广播 | 主机→电机 | 多轴控制(64B) |
| 0x210 | MIT 多轴 | 主机→电机 | MIT 多轴(64B) |
| 0x300+ID | 反馈帧 | 电机→主机 | 位置/速度/电流/力矩(12B) |

### 关键对象字典（OD）

| OD | 含义 | 初始化用法 |
|----|------|-----------|
| 0x1017 | 心跳周期 | SDO 写 heartbeat_ms，验证在线 |
| 0x2650 | 看门狗/限位 | SDO 写 1 关看门狗 |
| 0x100A | 固件版本 | SDO 读，验证链路 |
| 0x6040 | Controlword | 0x06/0x07/0x0F 使能 |
| 0x6041 | Statusword | 状态反馈 |
| 0x6081/0x6083/0x6084 | 速度/加速度/减速度 | 轮廓运动参数 |
| 0x1800/0x1A00 | TPDO 配置/映射 | 同步上报配置 |
