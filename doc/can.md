## 程序启动控制指令与返回流程图

> ✅ = 电机必须应答；⚠️ = best-effort（无应答/失败不阻塞主流程）。

```mermaid
graph TB
    A["上电 / 开机"] --> B["等 Bootup 帧<br/>0x701:00（⚠️可错过，不阻塞）"]
    B --> C["NMT Start<br/>cansend can0 000#0101<br/>⚠️ 无应答帧"]
    C --> D["SDO 写心跳 0x1017<br/>601#2B171000D0070000"]
    D --> D1{"电机回 0x581 写确认?<br/>581#6017100000000000"}
    D1 -- "❌ 超时/无应答" --> FAIL1["启动失败<br/>motor_startup_full return"]
    D1 -- "✅ 回 0x60" --> E["SDO 写 0x2650 看门狗<br/>SDO 读 0x100A 固件<br/>SDO 写 0x6081/6083/6084<br/>（⚠️ 全部 best-effort）"]
    E --> F["SDO 使能三步 0x6040<br/>06 → 07 → 0F<br/>各隔 20ms"]
    F --> F1{"每步都回 0x60 确认?<br/>581#6040600000000000"}
    F1 -- "❌ 任一步失败" --> FAIL2["启动失败"]
    F1 -- "✅ 三步都确认" --> G["配置 TPDO 映射<br/>（⚠️ best-effort）"]
    G --> H["启动完成 → 进入主循环<br/>每 50ms 轮询"]
    H --> I["SDO 读状态字 0x6041<br/>601#4001600000000000"]
    I --> I1{"电机回 0x4B 读应答?<br/>581#4B4160 状态字 0000"}
    I1 -- "❌ 无应答" --> OFFLINE["判 UNKNOWN → 电机离线"]
    I1 -- "✅ 回 0x4B" --> J{"状态字 ≥ 0x60?<br/>正常 0x27 = Operation enabled"}
    J -- "❌ 0x00 = NOT_READY" --> OFFLINE
    J -- "✅ 在线" --> K["motor_online 置位"]
    K --> L{"所有电机都在线?"}
    L -- "❌ 部分在线" --> T["等 5s 超时降级<br/>以当前在线数进入 READY"]
    L -- "✅ 全部在线" --> M
    T --> M["进入 READY<br/>开始接受 demo_algo 控制"]
```

## 关键应答要求

| 环节 | 程序下发 | 电机必须返回 | 不返回的后果 |
|------|---------|-------------|-------------|
| 写心跳 0x1017 | `601#2B171000D0070000` | `581#6017100000000000`（0x60 写确认） | 启动直接失败 |
| 使能 06/07/0F | `601#2B40600006000000` 等 | 每步 `581#6040600000000000`（0x60） | 启动直接失败 |
| 读状态字 0x6041 | `601#4001600000000000` | `581#4B4160<状态字小端>0000`（0x4B 读应答） | 判离线，不进 READY |
| 状态字值 | — | ≥ `0x60`（正常 `0x27`） | `0x00` 判 NOT_READY，同样离线 |

## 一句话总结

电机在「写参数时回 `0x60`、读状态字时回 `0x4B` + 状态字 `0x27`」这两点上做到，
程序就能从开机一路走到 READY 并接受 demo_algo 控制。
